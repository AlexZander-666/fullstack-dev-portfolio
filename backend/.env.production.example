# 生产环境配置 - Elastic Beanstalk
# 复制此文件为 .env.production 并填入实际值

# 服务器配置
PORT=5000
NODE_ENV=production

# MongoDB Atlas 配置
# 格式: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/personal-website?retryWrites=true&w=majority

# JWT 配置 - 生产环境请使用强随机密钥
# 可用命令生成: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-random-jwt-secret-key-at-least-64-characters
JWT_EXPIRES_IN=7d

# CORS 配置 - 你的前端域名
CORS_ORIGIN=https://www.zbw.life
